apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hswp-mysql-pv-claim
  labels:
    app: hswp
spec:
  storageClassName: slow
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
