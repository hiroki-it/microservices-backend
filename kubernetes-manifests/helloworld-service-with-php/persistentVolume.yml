apiVersion: v1
kind: PersistentVolume
metadata:
  name: hswp-mysql-pv
  labels:
    app: hswp
spec:
  storageClassName: slow
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/mysql
