apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: swp-mysql-pv-claim
  labels:
    app: swp
spec:
  storageClassName: slow
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
